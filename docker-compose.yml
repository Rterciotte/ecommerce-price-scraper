services:

  scraper-dashboard:

    build: .

    container_name: ecommerce-dashboard

    ports:
      - "8501:8501"

    volumes:
      - ./data:/app/data
      - ./output:/app/output

    restart: unless-stopped